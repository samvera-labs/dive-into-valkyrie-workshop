<%= form_with(model: @change_set.model) do |form| %>
  <% if @change_set.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@change_set.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% @change_set.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :author, style: "display: block" %>
    <%= form.text_field :author %>
  </div>

  <div>
    <%= form.label :series, style: "display: block" %>
    <%= form.text_field :series %>
  </div>

  <div>
    <%= form.label :alternate_ids, style: "display: block" %>
    <%= form.text_field :alternate_ids %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
